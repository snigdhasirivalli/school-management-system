{
  "buildCommand": "if [ -d \"frontend\" ]; then cd frontend && npm install && npm run build && cd .. && cp -r frontend/dist ./dist; else npm install && npm run build; fi",
  "outputDirectory": "dist"
}
